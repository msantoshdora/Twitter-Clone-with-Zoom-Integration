<% provide(:title, 'Topics') %>
<h1>TOPICS</h1>

<ul class="users">
  <%= render @topics %>
</ul>
 <%= render 'add_topic' if logged_in? && @user.admin?%>

<!-- <table>
<% @topics.each do |topic| %>
	<tr>
      <td><%= topic.name %></td>
      <td><%= pluralize(topic.users.count, "Mentor") %> </td>
      
    </tr>
  <% end %>
</table> -->