<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %> <!-- Rails knows to call the micropost partial because each element of @feed_items has class Micropost -->
  </ol>
  <%= will_paginate @feed_items %>
<% end %>